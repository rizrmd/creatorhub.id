PORT=8080
DATABASE_URL=postgres://postgres:postgres@localhost:5432/creatorhub?sslmode=disable
STATIC_DIR=../frontend/dist
JWT_SECRET=change-me-in-production
# Optional: enable HTTP basic auth (sandbox only — leave empty in production)
# BASIC_AUTH_USER=creatorhub
# BASIC_AUTH_PASS=change-me
